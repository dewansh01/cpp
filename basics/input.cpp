#include<bits/stdc++.h>
using namespace std;
int main(){
    ios::sync_with_stdio(0); //when program bottlenecks due to input and output this code will optimize it.
    cin.tie(0);
    int n;
    cin>>n;
    cout<<n;
}
//cer-un buffered error stream
//clog bufferd error stream